spring:
  flyway:
    enabled: true
  application:
    name: Users API
  datasource:
    url: "jdbc:postgresql://localhost:5432/users-api_db?currentSchema=main"
    username: "test"
    password: "123"
    driver-class-name: org.postgresql.Driver
server:
  port: 8081
